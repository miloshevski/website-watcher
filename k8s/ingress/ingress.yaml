apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: website-watcher-ingress
  namespace: website-watcher
spec:
  ingressClassName: nginx
  rules:
    - host: website-watcher.local
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 8080
          - path: /internal
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 8080
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 3000
